export class Day {
  constructor(
    public id: number,
    public dates: String,
    public start: String,
    public finish: String,
    public working_hours: number,
    public project_hours: number
    ) {
  }
}
